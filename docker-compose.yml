version: '3.8'

services:
  qrcode-service:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - HOST=0.0.0.0
      - UPLOAD_FOLDER=/app/uploads
      - BASE_URL=http://localhost:5000
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
